// Global setup for Jest
// e.g. Mocking console.log to keep test output clean
// jest.spyOn(global.console, 'log').mockImplementation(() => jest.fn());

// Timeout configuration if needed
jest.setTimeout(10000);
