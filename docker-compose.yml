version: '3.8'

services:
  talklink:
    build: .
    image: talklink:latest
    container_name: talklink
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./.env:/app/.env
    environment:
      - PORT=3000
